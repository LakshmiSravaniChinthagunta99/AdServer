package com.ad.campaign.test;

import static com.jayway.restassured.RestAssured.expect; 
import groovyx.net.http.ContentType;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;

import com.jayway.restassured.RestAssured;

//NOTE THis test is not in working condition
/**
 * Basic integration tests for service demo application.
 *
 * @author 
 */
/*
@RunWith(SpringRunner.class)
@SpringBootTest(classes = AdServerConfiguration.class, webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@TestPropertySource(properties = {"management.port=0"})
*/
public class AdServerTest {
/*
	@Before
    public void setUp(){
        RestAssured.basePath = "http://localhost:8080";
    }

    @Test
    public void testGetProducts(){
        expect().statusCode(200).contentType(ContentType.JSON).when()
                .get("/getProducts/companyid/companyname/12345088723");
    }*/

}
